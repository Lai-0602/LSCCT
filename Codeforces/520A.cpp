#include <bits/stdc++.h>
#define int long long
using namespace std;
signed main() {
    freopen("520A.in", "r", stdin); freopen("520A.out", "w", stdout);
    cin n;
    int p[25];
    int a[n - 1];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    
    return 0;
}