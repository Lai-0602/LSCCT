#include <bits/stdc++.h>
#define int long long
using namespace std;
int32_t main() {
    int N, T, P;
    cin >> N >> T >> P;
    struct every {
        bool solve;
    }
    for (int i = 0; i < N; i++) {
        cin >> every.solve;
    }
}